[package]
name = "liblkl2"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
anyhow = "1.0.100"
env_logger = "0.11"
flutter_rust_bridge = "=2.11.1"
log = "0.4"
once_cell = "1.21.3"
parking_lot = "0.12"
libparser = { path = "../libparser" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(frb_expand)'] }
